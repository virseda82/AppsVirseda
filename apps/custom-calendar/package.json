{
  "name": "custom-calendar",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c green,blue \"npm --prefix api start\" \"npm --prefix web run dev\"",
    "dev:api": "npm --prefix api start",
    "dev:web": "npm --prefix web run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
